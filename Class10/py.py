# 建立一個空的購物清單（用來裝要買的東西）
shopping_list = []


# 這個功能是「顯示購物清單」
def show_list():
    print("\n📋 目前購物清單：")  # 顯示標題
    if not shopping_list:  # 如果清單是空的
        print("（清單是空的）")  # 告訴使用者沒有東西
    else:  # 如果清單有東西
        for i, item in enumerate(shopping_list):  # 一個一個顯示
            print(i, ".", item)  # 顯示編號和內容
    print("--------------------")  # 分隔線


# 程式一直重複執行
while True:

    show_list()  # 先顯示目前的購物清單

    # 顯示功能選單
    print("請選擇要做的事：")
    print("1 新增東西")
    print("2 修改東西")
    print("3 刪除東西")
    print("4 離開程式")

    # 讓使用者輸入選項
    choice = input("輸入 1~4：")

    # ===== 新增東西 =====
    if choice == "1":
        print("新增方式：")
        print("1 加到最後（append）")
        print("2 插在指定位置（insert）")

        add_choice = input("選擇 1 或 2：")  # 選新增方式

        if add_choice == "1":
            item = input("輸入要新增的東西：")  # 輸入東西
            shopping_list.append(item)  # 加到清單最後

        elif add_choice == "2":
            item = input("輸入要新增的東西：")  # 輸入東西
            index = int(input("輸入要放的位置："))  # 輸入位置
            shopping_list.insert(index, item)  # 插入指定位置

    # ===== 修改東西 =====
    elif choice == "2":
        index = int(input("輸入要修改的編號："))  # 輸入要改哪一個
        if 0 <= index < len(shopping_list):  # 檢查編號有沒有超出
            new_item = input("輸入新的內容：")  # 輸入新內容
            shopping_list[index] = new_item  # 直接換掉
        else:
            print("編號錯誤")  # 編號不對

    # ===== 刪除東西 =====
    elif choice == "3":
        print("刪除方式：")
        print("1 用名稱刪除（remove）")
        print("2 用位置刪除（pop）")

        del_choice = input("選擇 1 或 2：")  # 選刪除方式

        if del_choice == "1":
            item = input("輸入要刪除的名稱：")  # 輸入名稱
            if item in shopping_list:  # 如果清單裡有
                shopping_list.remove(item)  # 用名稱刪掉
            else:
                print("清單裡沒有這個東西")  # 找不到

        elif del_choice == "2":
            index = int(input("輸入要刪除的位置："))  # 輸入位置
            if 0 <= index < len(shopping_list):  # 檢查位置
                shopping_list.pop(index)  # 用位置刪掉
            else:
                print("位置錯誤")  # 位置不對

    # ===== 離開程式 =====
    elif choice == "4":
        print("👋 掰掰～下次再來買！")  # 說再見
        break  # 結束程式

    # ===== 輸入錯誤 =====
    else:
        print("請輸入正確的選項")  # 提醒輸入錯誤
